{
  "name": "presburger-mdbook",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "verify": "node verify-contract.mjs",
    "build": "node build.mjs",
    "ci": "npm run test && npm run verify && npm run build"
  },
  "devDependencies": {
    "katex": "0.16.22",
    "mermaid": "11.16.0"
  }
}
